# Follow-ups 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the deferred leftovers: import/sync edges, offline polish, tags polish.

**Architecture:** Small, independent changes in existing modules; no schema change.

**Tech Stack:** Rust (axum, sqlx `Any` on SQLite + PostgreSQL), Svelte 5 runes, TypeScript, Vitest, Playwright.

**Spec:** `docs/superpowers/specs/2026-09-14-follow-ups-2-design.md`

## Global Constraints

- Branch `build-follow-ups-2` from `main`; never commit to `main`. Before every commit `git branch --show-current` must print `build-follow-ups-2`. Stage only files the task changes.
- Portable SQL; PostgreSQL runs use a throwaway `postgres:17` container that is always stopped.
- Every UI string in en and de; German Intl output asserted by pattern.
- Playwright with project defaults (no `--workers`); tests that rely on `page.route` for cached API paths use `test.use({ serviceWorkers: 'block' })`.
- Foreground commands (timeouts up to 600000 ms); never end a turn while one runs. Never weaken an assertion. Comments explain *why*.
- Commits end with exactly:
  ```
  Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01DNUZLftSTtND7ABvN6eoGv
  ```

---

### Task 1: Import counts for types, and sync refuses edits to deleted rows

**Files:** `src/api/export.rs`, `src/sync/apply.rs`, `tests/export.rs`, `tests/sync.rs`, `docs/openapi.json`, `docs/superpowers/specs/2026-09-08-offline-sync-design.md`, `frontend/src/lib/types.ts`, `frontend/src/routes/settings/Data.svelte`, `frontend/src/i18n/en.ts`, `frontend/src/i18n/de.ts`

- [ ] **Step 1: Failing tests.**
  - `tests/export.rs`: an import into an account that already has a type named like one archive type and not the other → response `types_created: 1`, `types_merged: 1`; an archive without types → both 0.
  - `tests/sync.rs`: for each of object, activity, reminder, attachment and object_type: create the row (REST or the file's helpers), delete it (REST or a delete op), then push a `set` op on a whitelisted text field → outcome rejected with reason `this item was deleted`, the stored value unchanged, and no new change row / field clock for that field (assert via pull or the file's existing helpers).
  Run → FAIL.
- [ ] **Step 2: Implement.**
  - `ImportCounts` gains `pub types_created: usize, pub types_merged: usize`, counted where archive types are mapped (created vs mapped onto an existing same-name type).
  - In `apply_op`'s `OpKind::Set` branch, before reading `field_clock`, look up the row by `client_uuid` in `op.entity.table()` (closed set) and reject with `Outcome::Rejected { reason: "this item was deleted".into() }` when `deleted_at IS NOT NULL`. Keep the existing ownership/unknown-uuid handling order intact (read where the op's ownership is established first and put the check right after it).
  - OpenAPI: document the two import count fields.
  - Offline-sync spec "Later additions": one bullet that a `set` on a deleted row is rejected with that reason.
  - Frontend: `ImportCounts` type gains the fields; `settings.import-done` becomes, en: `'Imported {objects} objects, {activities} activities, {attachments} files, {reminders} reminders, {types_created} types ({types_merged} merged with existing ones)'`; de: `'{objects} Objekte, {activities} Aktivitäten, {attachments} Dateien, {reminders} Erinnerungen, {types_created} Typen ({types_merged} mit vorhandenen zusammengeführt) importiert'`.
- [ ] **Step 3: Verify.** `cargo test --test export --test sync`; PostgreSQL (port 55445) `--test export --test sync`; `cargo test`; `cargo clippy --all-targets -- -D warnings`; `cd frontend && npm run check && npx vitest run`.
- [ ] **Step 4: Commit** — `fix: import counts own types, and sync refuses edits to deleted items`.

---

### Task 2: Offline polish

**Files:** `frontend/src/lib/api.ts`, `frontend/src/stores/session.ts`, `frontend/src/lib/cache-owner.ts`, `frontend/src/lib/TopBar.svelte`, `frontend/src/lib/SignedIn.svelte`, `frontend/src/routes/settings/Account.svelte`, `frontend/src/i18n/en.ts`, `frontend/src/i18n/de.ts`, tests in `frontend/tests/` (`api.test.ts`, `session.test.ts`, `cache-owner.test.ts`), `frontend/tests-e2e/25-offline-cache.spec.ts`, `docs/superpowers/specs/2026-09-14-offline-api-cache-design.md`

- [ ] **Step 1: Check the Date header.** Run the server (as e2e does, or `cargo run` against a scratch data dir) and confirm a `GET /api/health` response carries `Date`. If it does not, add it server-side in the one place responses are built (a tower layer) with a test, and say so.
- [ ] **Step 2: Failing tests.**
  - `api.test.ts`: a pure helper `servedFromCache(dateHeader: string | null, sentAt: number, now?: number): boolean` — null → false; a Date 5 s before `sentAt` → false; 61 s before → true; unparsable → false. And the `servingSaved` store flips true on such a response and back to false on the next fresh one (mock `fetch`).
  - `session.test.ts`: in offline mode a 30 s interval retries `loadSession` (fake timers) until the session is known, then stops; `logout()` rejects with a connectivity error → the caller-facing helper `signOutErrorMessage(e)` returns the `nav.signout-offline` key for a `TypeError`-style network failure and the server message for an `ApiError`.
  - `cache-owner.test.ts`: the profile round-trips `currency`; a stored profile without `currency` still reads (currency undefined).
  - `session.test.ts`: offline start sets `currency` from the profile when present.
- [ ] **Step 3: Implement** per spec A1–A4. The TopBar shows the note when `$offline || $servingSaved`. `rememberProfile` is called again after `/settings` loads with the currency.
- [ ] **Step 4: E2E.** In `25-offline-cache.spec.ts` add an e2e for A2: with the service worker active and the user signed in, `context.setOffline(true)`, click sign out → the text `Signing out needs a connection.` shows and the user stays on a signed-in page. For A1, try `context.route('**/api/objects*', ...)` delaying past 4 s (Playwright's `context.route` can intercept service-worker requests in Chromium when `PW_EXPERIMENTAL_SERVICE_WORKER_NETWORK_EVENTS=1`); if that cannot be made reliable, rely on the unit tests and state that in the report — do not add a flaky test.
- [ ] **Step 5: Verify.** `cd frontend && npm run check && npx vitest run`; `npm run e2e -- 25-offline-cache 13-shell 01-smoke 14-settings`; spec updated (A1–A4 described under "Starting offline").
- [ ] **Step 6: Commit** — `feat: the app says when it shows saved data, signs out honestly offline, and leaves offline mode on its own`.

---

### Task 3: Tags polish

**Files:** `frontend/src/app.css`, `frontend/src/lib/TagInput.svelte`, `frontend/src/lib/tags.ts`, `frontend/tests/tags.test.ts`, `frontend/tests-e2e/23-tags.spec.ts`

- [ ] **Step 1: Failing tests.**
  - `tags.test.ts`: `splitTyped(tags, value)`:
    - `(['A'], 'b,')` → `{ tags: ['A','b'], text: '', error: null }`
    - `([], 'a, b ,c')` → `{ tags: ['a','b'], text: 'c', error: null }`
    - `(['Winter'], 'winter,x')` → `{ tags: ['Winter'], text: 'x', error: null }` (dedupe, no error)
    - `([], 'x'.repeat(33) + ',ok,')` → `{ tags: ['ok'], text: 'xxx…(33)', error: 'too-long' }` (failing segment stays in text)
    - `(ten tags, 'eleven,')` → `{ tags: ten tags, text: 'eleven', error: 'too-many' }`
    - no comma → unchanged tags, text = value, error null.
  - `23-tags.spec.ts`: on the new-object form, focus the empty Tags field and press Enter → the form saves (URL becomes `/objects/<id>`); on another new object, type 33 characters in Tags and press Enter → the error text is visible, the input's `aria-describedby` names the error element's id, and that element has `aria-live="polite"`.
- [ ] **Step 2: Implement.** `splitTyped` in `tags.ts` (reusing `addTag`); `TagInput.svelte`'s `oninput` calls it and applies `{ tags, text, error }` (translating the error key). CSS: `button.tag::before` becomes `top: calc(50% - 16px); bottom: calc(50% - 16px)` (keep left/right), and the remove button's `::before` in `TagInput.svelte` uses the same 32 px vertical extent — comment updated to cite WCAG 2.2 AA 24×24.
- [ ] **Step 3: Verify.** `cd frontend && npm run check && npx vitest run`; `npm run e2e -- 23-tags 22-objects-list`; 390px screenshot of an objects list with tagged cards saved to `/home/ben/repo/logb/.superpowers/sdd/fu2-tags-390.png` and looked at.
- [ ] **Step 4: Full suite** `cd frontend && npm run e2e`.
- [ ] **Step 5: Commit** — `fix: tag tap areas stay on their chip, and the tag input's comma, Enter and error handling are tested`.
